services:
  diarizer:
    build:
      context: ./backend/diarizer-service
    ports:
      - "8790:8790"
    environment:
      HF_TOKEN: ${HF_TOKEN}
      PYANNOTE_MODEL: ${PYANNOTE_MODEL:-pyannote/speaker-diarization-3.1}
      DIARIZER_API_KEY: ${DIARIZER_API_KEY:-}
